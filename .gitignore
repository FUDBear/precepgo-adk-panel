# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
*.egg-info/
dist/
build/

# Environment variables and secrets
.env
*.key
*.pem
*.log
server.log

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Google Cloud
.gcloud/
gcloud-credentials.json
application_default_credentials.json

# Temporary files
*.tmp
/tmp/
*.bak

# Don't commit API keys or credentials
*API_KEY*
*api_key*
credentials/
secrets/

# Project docs to exclude from Git history/uploads
docs/
BARASH_INTEGRATION.md
BARASH_ONLY_VERIFICATION.md

# Prevent committing actual API keys in documentation files
# (SECURITY.md should use placeholders only)
# Note: SECURITY.md itself can be committed, but must not contain real keys

# Large data files - ignore text files in data directory (Barash content)
data/**/*.txt
data/*.txt

# But allow JSON files needed by agents
# Note: We don't ignore data/ directory itself, just specific file types
# This allows JSON files to be tracked while ignoring large text files

# Vector DB setup scripts and generated files
vectordb/

# ADK Python reference source code (large folder, don't commit)
context/adk-python-main/

# Test files and utilities (not needed for production)
tests/

