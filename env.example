# Google Gemini API Key (required)
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Vector Search Configuration (default: true - uses Vertex AI Vector Search)
# Set to "false" to use legacy MCP server instead
USE_VECTOR_SEARCH=true

# MCP Server URL (optional - fallback only if USE_VECTOR_SEARCH=false)
# Legacy MCP server (deprecated in favor of Vector Search)
MCP_URL=https://your-mcp-server.run.app

# Google Cloud Project (required for Vector Search)
GOOGLE_CLOUD_PROJECT=precepgo-mentor-ai
GOOGLE_CLOUD_REGION=us-central1

# Firebase/Firestore Configuration
# Set this to your Firebase project ID (from Firebase Console)
# This is the project ID from your Firebase config (e.g., auth-demo-90be0)
FIREBASE_PROJECT_ID=auth-demo-90be0
# Note: If FIREBASE_PROJECT_ID is not set, will use GOOGLE_CLOUD_PROJECT

# Optional: Service Account Key File (for service account authentication)
# If using a service account key file, set the path here:
# GOOGLE_APPLICATION_CREDENTIALS=./service-account-key.json
# Leave unset to use Application Default Credentials (ADC)

# Firebase Storage Configuration (optional - auto-detected if not set)
# If not set, will use {FIREBASE_PROJECT_ID}.appspot.com as default bucket
# STORAGE_BUCKET_NAME=your-project-id.appspot.com
# Or use a full URL:
# STORAGE_BUCKET_URL=gs://your-project-id.appspot.com/agent_assets

